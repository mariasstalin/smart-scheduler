
spring:
  application:
    name: gateway
  cloud:
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true

          routes:
            - id: user
              uri: lb://user
              predicates:
                - Path=/api/v1/user/**
            - id: appointment
              uri: lb://appointment
              predicates:
                - Path=/api/v1/appointment/**
            - id: notification
              uri: lb://notification
              predicates:
                - Path=/api/v1/notification/**
            - id: audit
              uri: lb://audit
              predicates:
                - Path=/api/v1/audit/**
