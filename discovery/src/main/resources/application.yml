
server:
  port: 8761
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    peer-node-read-timeout-ms: 10000

spring:
  application:
    name: discovery
management:
  endpoints:
    web:
      exposure:
        include: health,info
