
server:
  port: 8082

spring:
  application:
    name: appointment

zoho:
  api:
    base-url: ${ZOHO_API_BASE_URL:https://www.zohoapis.in}

management:
  endpoints:
    web:
      exposure:
        include: health,info
