
server:
  port: 9999
  servlet:
    context-path: /demo

spring:
  application:
    name: demo
  thymeleaf:
    cache: false
  redis:
    host: localhost
    port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health,info

message:
  system-phone: 919999999999