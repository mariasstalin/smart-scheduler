
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery:8761/eureka/

spring:
  cloud:
    config:
      server:
        native:
          searchLocations: file:./config-repo
  redis:
    host: redis
    port: 6379