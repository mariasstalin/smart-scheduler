
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  cloud:
    config:
      server:
        native:
          searchLocations: file:./config-repo
  data:
    redis:
      host: redis
      port: 6379
      timeout: 2000ms
